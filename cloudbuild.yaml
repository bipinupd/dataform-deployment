steps:
  - id: dataform
    name: node
    entrypoint: sh
    args:
    - '-c'
    - |
        npm i -g -quiet @dataform/cli \
        && envsubst < dataform.json \
        && envsubst < deploy.sh \
        && sh deploy.sh